# QA Environment Configuration
# This file is a template — copy to .env or set ENVIRONMENT=qa

# Database — USE SEPARATE QA DATABASE
DATABASE_URL="postgresql://ocr_user:<QA_DB_PASSWORD>@<QA_DB_HOST>:5432/ocr_portal_qa?schema=public"

# Server
NODE_ENV=production
PORT=3000

# Logging
LOG_LEVEL=info

# OCR (tune to QA server CPU capacity)
OCR_WORKERS=4
MAX_CONCURRENT_BATCHES=4

# File Watcher
WATCH_FOLDER_PATH=./watch
TIFF_STORAGE_PATH=./storage/tiffs
ARCHIVE_FOLDER_PATH=./storage/archive

# Authentication — GENERATE UNIQUE SECRETS FOR QA
JWT_SECRET=<generate-with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))">
JWT_REFRESH_SECRET=<generate-with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))">
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Frontend URL (for CORS) — UPDATE WITH QA IP/HOSTNAME
FRONTEND_URL=http://<QA_HOSTNAME>

# OIDC / Entra ID (update redirect URI in Azure Portal for QA)
# ENTRA_CLIENT_SECRET=<from-azure-portal>
