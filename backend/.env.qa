# QA Environment Configuration — qa-aiops-01 (172.16.20.204)
# Used by: deploy.ps1 -Environment qa

# Database (PostgreSQL on QA server)
DATABASE_URL="postgresql://ocr_user:ocr_password@localhost:5432/ocr_portal_qa?schema=public"

# Server
NODE_ENV=production
PORT=3000

# Logging
LOG_LEVEL=info

# OCR (tune to QA server CPU capacity)
OCR_WORKERS=4
MAX_CONCURRENT_BATCHES=4

# File Watcher
WATCH_FOLDER_PATH=./watch
TIFF_STORAGE_PATH=./storage/tiffs
ARCHIVE_FOLDER_PATH=./storage/archive

# Authentication
JWT_SECRET=1aa5d05673385bb5669cf88089fdfdd45151a0f42ce3873358b5e93c56f6c758
JWT_REFRESH_SECRET=2fc13d40eee409fe9030f9d5fd634c493a1436dfe1c23f7055938525446d2fa4
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Frontend URL (for CORS) — backend serves frontend on same port
FRONTEND_URL=http://172.16.20.204:3000

# OIDC / Entra ID (update redirect URI in Azure Portal for QA)
# ENTRA_CLIENT_SECRET=<from-azure-portal>
